# tasks file for core::rhel/yum_install

- name: Install a list of required packages
  yum:
    name: "{{ packages }}"
  vars:
    packages:
    - rsync
    - net-tools
    - which
    - tree
    - screen
    - vim
    - nano